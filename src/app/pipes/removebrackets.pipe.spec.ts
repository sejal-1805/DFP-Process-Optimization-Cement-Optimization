import { RemovebracketsPipe } from './removebrackets.pipe';

describe('RemovebracketsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovebracketsPipe();
    expect(pipe).toBeTruthy();
  });
});
